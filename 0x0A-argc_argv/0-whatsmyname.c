#include <stdio.h>

/**
 * main - prints name followed by a new line
 * @argc: number of arguments
 * @argv: points to strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
